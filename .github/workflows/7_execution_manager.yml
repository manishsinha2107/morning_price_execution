name: 9:15 AM Execution Manager

on:
  # Allows you to trigger the run manually from the GitHub Actions UI
  workflow_dispatch:
  
  # Opens a secure webhook listener for cron-job.org
  # Trigger via POST to: https://api.github.com/repos/YOUR_USERNAME/YOUR_REPO/dispatches
  # with JSON body: {"event_type": "market_open_execution"}
  repository_dispatch:
    types: [market_open_execution]

jobs:
  execute-trades:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip' # Speeds up future runs
          
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          
      - name: Run Execution Manager
        env:
          # Securely inject secrets into the runtime environment
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          FYERS_USERNAME: ${{ secrets.FYERS_USERNAME }}
          FYERS_APP_ID: ${{ secrets.FYERS_APP_ID }}
          FYERS_SECRET_ID: ${{ secrets.FYERS_SECRET_ID }}
          FYERS_PIN: ${{ secrets.FYERS_PIN }}
          FYERS_TOTP_KEY: ${{ secrets.FYERS_TOTP_KEY }}
        run: |
          python 7_execution_manager.py
